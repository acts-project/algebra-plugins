set (BENCHMARK_ENABLE_INSTALL OFF)
set (BENCHMARK_ENABLE_TESTING OFF)
add_subdirectory("${PROJECT_SOURCE_DIR}/extern/benchmark" "extern/benchmark")

macro(add_detray_benchmark TESTNAME FILES PLUGIN_LIBRARY)
    add_executable(${TESTNAME} ${FILES})
    target_link_libraries(${TESTNAME} benchmark algebra::tests_common ${PLUGIN_LIBRARY})
    install(TARGETS ${TESTNAME} RUNTIME DESTINATION bin)
endmacro()

add_subdirectory(core)
if(ALGEBRA_PLUGIN_INCLUDE_ARRAY_PLUGIN)
    add_subdirectory(array)
endif()
if(ALGEBRA_PLUGIN_INCLUDE_EIGEN_PLUGIN)
    add_subdirectory(eigen)
endif()
if(ALGEBRA_PLUGIN_INCLUDE_SMATRIX_PLUGIN)
    add_subdirectory(smatrix)
endif()
